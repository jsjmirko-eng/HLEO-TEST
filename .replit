modules = ["python-3.12", "web", "bash"]
run = "DATABASE_URL=sqlite:///./hleo.db python -m uvicorn api.main:app --host 0.0.0.0 --port 8000"

[[ports]]
localPort = 8000
externalPort = 80

[nix]
channel = "stable-25_05"

[agent]
expertMode = true

[workflows]
runButton = "Project"

[[workflows.workflow]]
name = "Project"
mode = "parallel"
author = "agent"

[[workflows.workflow.tasks]]
task = "workflow.run"
args = "Start application"

[[workflows.workflow]]
name = "Start application"
author = "agent"

[[workflows.workflow.tasks]]
task = "shell.exec"
args = "DATABASE_URL=sqlite:///./hleo.db python -m uvicorn api.main:app --host 0.0.0.0 --port 8000"
waitForPort = 8000

[workflows.workflow.metadata]
outputType = "console"
